{
  "name": "murall-emotes-polygon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ murall-art/murall-emotes-polygon",
    "create-local": "graph create --node http://localhost:8020/ murall-art/murall-emotes-polygon",
    "remove-local": "graph remove --node http://localhost:8020/ murall-art/murall-emotes-polygon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 murall-art/murall-emotes-polygon",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
